"use client";
import {useEffect,useState} from "react";
export default function Announcements(){const[data,setData]=useState<any[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/discord/announcements',{cache:'no-store'}).then(r=>r.json()).then(d=>setData(d.announcements||[])).finally(()=>setLoading(false))},[]);return <main style={{minHeight:'100vh',background:'#070707',color:'#f2f0ea',padding:'110px 7vw',fontFamily:'DM Sans,Arial'}}><div style={{maxWidth:1000,margin:'auto'}}><small style={{letterSpacing:'.18em',color:'#777'}}>FBMRP / PUBLIC FEED</small><h1 style={{fontSize:'clamp(48px,8vw,100px)',lineHeight:.9,letterSpacing:'-.06em'}}>Announcements.</h1><p style={{color:'#777',maxWidth:560,lineHeight:1.7}}>Live updates pulled directly from the official public announcement channel.</p><div style={{marginTop:50,borderTop:'1px solid #252525'}}>{loading?<p style={{padding:'30px 0',color:'#777'}}>Loading live feed…</p>:data.length===0?<p style={{padding:'30px 0',color:'#777'}}>No public announcements yet.</p>:data.map(m=><article key={m.id} style={{padding:'28px 0',borderBottom:'1px solid #252525'}}><small style={{color:'#666'}}>{new Date(m.createdAt).toLocaleString()}</small><p style={{whiteSpace:'pre-wrap',fontSize:16,lineHeight:1.7}}>{m.content||'Media update'}</p>{m.attachments?.map((a:any)=><img key={a.url} src={a.url} alt={a.name} style={{maxWidth:'100%',maxHeight:500,objectFit:'contain',marginTop:12}}/>)}</article>)}</div></div></main>}
