import { StoreClient } from './StoreClient'

export const metadata = {
  title: 'FBMR Store',
  description: 'Official FBMR store and community marketplace.',
}

export default function StorePage() {
  return <StoreClient />
}
